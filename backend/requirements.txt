fastapi==0.115.6
uvicorn[standard]==0.34.0
websockets==14.1
google-genai>=1.65.0
python-dotenv==1.0.1
asyncpg==0.30.0
pgvector==0.3.6

# Scraper pipeline
yt-dlp>=2024.1.1
youtube-transcript-api>=0.6.2
pyyaml>=6.0
aiofiles>=23.0

# Music / transcoding support
mutagen>=1.47
pyacoustid>=1.3.0
httpx[socks]>=0.27.0
python-multipart>=0.0.9

# Audio analysis (Sprint 11)
librosa>=0.10
numpy>=1.26
soundfile>=0.12
scipy>=1.12

# ML models (Sprint 11 — AST + PANNs)
transformers>=4.40
torch>=2.2
torchaudio>=2.2

# PANNs audio embeddings (Sprint 11 S11.3)
# panns_inference provides CNN14 pre-trained on AudioSet (2048-dim embeddings).
# Falls back automatically to raw-torch CNN14 with downloaded weights if unavailable.
panns-inference>=0.1

# Broadcast — YouTube chat bridge
google-api-python-client>=2.0
google-auth-oauthlib>=1.0
google-auth-httplib2>=0.2

# Testing
pytest>=8.0
pytest-asyncio>=0.23
# httpx already listed above — used by both source clients and test ASGI transport
